services:
  - type: web
    name: portfolio-web
    env: node
    plan: free
    region: oregon
    repo: https://github.com/d8371962-eng/portfolio.git
    branch: main
    autoDeploy: true
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production